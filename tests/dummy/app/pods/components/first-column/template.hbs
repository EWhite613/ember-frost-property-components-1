<div class='ne-icon'>
  {{component svg model=model}}
</div>
<div class="block column">
  {{#if primaryId}}
    {{#if (is-string primaryId)}}
      {{primary-identifier value=(get model primaryId)}}
    {{else}}
      {{component primaryId model=model}}
    {{/if}}
  {{/if}}
  {{#if secondaryId}}
    {{#if (is-string secondaryId)}}
      {{secondary-identifier value=(get model secondaryId)}}
    {{else}}
      {{component secondaryId model=model}}            
    {{/if}}
  {{/if}}
</div>

{{#if isExpanded}}
      <div class="block start frost-flex-1">
        {{!-- <div class='tertiary list-value'> {{#if model.projectID}}{{model.projectID}}{{else}}none{{/if}}</div>--}}
        <div class='first-col'>
            <div>Extra stuff</div>
            <div>Extra stuff</div>
            <div>Extra stuff</div>
            <div>Extra stuff</div>
        </div>
      </div>
{{/if}}